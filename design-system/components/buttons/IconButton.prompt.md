Icon-only button for save / share / close and toolbar actions — 40×40 with a light-gray hover circle. Always pass `label`.

```jsx
<IconButton label="Save"><HeartIcon /></IconButton>
<IconButton label="Share" active><ShareIcon /></IconButton>
```

`active` turns the icon Zillow blue. Color follows text by default.
