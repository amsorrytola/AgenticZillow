The one branded AI entry point — a blue→violet ✦ gradient pill. Reserve the agentic gradient for this component only; everything else stays Zillow blue.

```jsx
<AskPill />                       {/* header pill */}
<AskPill fab badge={3} />         {/* floating launcher, bottom-right */}
```

In the header it sits in the right cluster; as a FAB it persists on scroll and shows a count for proactive items.
